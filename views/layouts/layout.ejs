<!-- views/layouts/layout.ejs -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>
        <%= title || 'GoFIT' %>
    </title>
    <!-- Example usage: in your route: res.render('page', { title: 'Register - GoFIT' }) -->
    <!-- Define custom title this -->
    <% // block('title', 'Register - GoFIT' ) %>

        <!-- Tailwind -->
        <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&family=Open+Sans&display=swap"
            rel="stylesheet" />

        <% // if (typeof stylesheets !=='undefined' ) { %>
            <% // stylesheets.forEach(css=> { %>
                <!-- <link rel="stylesheet" href="<%// css %> " /> -->
                <% // }); %>
                    <% //} %>
                        <!-- 
        In controller: 
        res.render('page', { stylesheets: ['/css/auth.css', 'https://example.com/style.css'] }) 
    -->
                        <!-- Add page-specific CSS like this -->
                        <% // stylesheets=[ '/css/auth.css', 'https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css' ] %>


                            <%- // block('headScripts') %>

    <style>
        .custom-blur {
            backdrop-filter: blur(6px);
            border: 1px solid rgba(209, 213, 219, 0.1);
            /* Semi-transparent gray border */
            /* Adjust the blur radius as needed */
        }
    </style>
</head>

<body class="font-sans text-gray-800">

    <%- include('../partials/navbar', { user }) %>

        <main>
            <%- body %>
        </main>

        <%- include('../partials/footer') %>

            <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>

            <% // if (typeof scripts !=='undefined' ){%>
                <% // scripts.forEach(js=> { %>
                    <!-- <script src="<%=// js %>" defer></script> -->
                    <% // }); %>
                        <% // } %>
                            <!-- 
        Example: 
        res.render('page', { scripts: ['/js/form.js'] })
    -->
                            <%- // block('footerScripts') %>
</body>

</html>